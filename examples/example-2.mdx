---
title: "Metaplex NFT metadata"
description: "Stream metadata from Metaplex mints and store it in ClickHouse for efficient indexing"
image: "https://images.unsplash.com/photo-1551288049-bebda4e38f71?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=800&q=80"
---

<Steps>
  <Step title="Setup">
    Step 1 description goes here.

    ```typescript
    // Step 1 code example
    const setup = () => {
      console.log("Setting up Example 2");
    };
    ```

  </Step>

  <Step title="Configuration">
    Step 2 description goes here.

    ```typescript
    // Step 2 code example
    const configure = () => {
      return {
        apiKey: "your-api-key",
        endpoint: "https://api.example.com"
      };
    };
    ```

  </Step>

  <Step title="Implementation">
    Step 3 description goes here.

    ```typescript
    // Step 3 code example
    const implement = async () => {
      const data = await fetchData();
      return processData(data);
    };
    ```

  </Step>

</Steps>

## Next Steps

After mastering these advanced patterns, you might want to:

- Explore [Example 3](/examples/example-3) for integration with other systems
- Learn about [specific advanced feature] in our [documentation link]
- Check out our [API Reference](/api-reference/introduction) for complete details on all available options
